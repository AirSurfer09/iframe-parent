/**
 * Runtime configuration placeholder.
 *
 * In the Docker image this file is REGENERATED on every container start from
 * environment variables (see docker/entrypoint.sh), which is what lets a client
 * repoint the build without rebuilding it. Empty values fall back to the values
 * baked in at build time.
 */
window.__CONVAI_CONFIG__ = {};
